/// <reference types="Cypress" />
import "cypress-file-upload"

describe("tables", () => {
  it("", () => {
    cy.visit("")
    // validacion de title

    cy.xpath("".click());

    // para definir alias

    cy.get("").should().as("nombredealias")
  })
})